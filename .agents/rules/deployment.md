# Autonomous Deployment Rule

- Always execute deployment (GitHub Pages, Render, Vercel, etc.) completely autonomously end-to-end.
- Do NOT instruct or delegate manual UI click steps on third-party platforms to the user if automated CLI or API commands can accomplish the deployment directly.
